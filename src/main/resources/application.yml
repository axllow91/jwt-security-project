jwt:
  secret: '[a-zA-Z0-9._]^+$Guidelines898979799887AlphabeticArraNumeralotherSymbol$'


server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supportportal?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
